# Example .env for the React client
# Copy to .env.production (or set REACT_APP_API_URL in Vercel project settings)

# URL of the deployed API server (no trailing slash)
REACT_APP_API_URL=https://your-api-domain.com
